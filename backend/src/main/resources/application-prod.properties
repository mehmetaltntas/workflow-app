# ===============================
# PRODUCTION PROFIL AYARLARI
# ===============================

# Hibernate - Production Ortamı (Flyway migrasyondan sonra validate çalışır)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Flyway - Production ortamda aktif
spring.flyway.enabled=true

# Logging - Production seviyesi
logging.level.root=WARN
logging.level.com.workflow.backend=INFO
logging.level.org.hibernate.SQL=WARN

# Swagger/OpenAPI - Production'da devre dışı
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Connection Pool - Production optimizasyonu
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
